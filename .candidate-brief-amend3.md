# Candidate-1 amendment pass 3 — one terminology fix, docstring only

Main has corrected an ambiguity in the legacy-seeding conversion I had you write. `F_analysis` in

    P_candidate = 2 * P_legacy_physical / F_analysis        (because A = sqrt(2 P))

**MUST mean the ANALYSIS SAMPLE RATE, 16000 Hz** — not an "analysis bandwidth". The current
wording in `src/experiments/stochastic_fit/revised_phase.py` (around lines 51-53) says
"the analysis bandwidth the legacy per-hertz density is expressed in", which a reader could take
as 7900 or 8000 Hz and so make a factor-two error.

## The change (one file, docstring only)

In `src/experiments/stochastic_fit/revised_phase.py`, in the "THE LEGACY CONVERSION, EXPLICITLY"
paragraph:

- Rename the symbol in the formula to `fs_analysis` so it cannot be read as a bandwidth, and give
  it its value explicitly: `fs_analysis = 16000 Hz`, the analysis SAMPLE RATE (`SR`), i.e. the rate
  `data.periodogram` reads at.
- Replace the "analysis bandwidth" sentence with wording that says, unambiguously, that this is the
  sample rate and NOT a band edge: state that it is 16000 Hz and explicitly that it is not 7900 Hz
  and not 8000 Hz, because using a band edge here produces a factor-two error.
- Keep the rest of the paragraph as it is, including the prohibition on seeding a candidate export
  from a legacy export without applying the conversion explicitly, and the `A = sqrt(2 P)` note.

Nothing else changes. No code, no other file, no mic-phase wording (Main has ruled that no further
mic-phase complexity is wanted).

## Hard rules

- Edit that one docstring paragraph in that one file. Touch nothing else.
- **No git mutation. Run nothing** — no pytest, no ruff, no mypy, no imports, no render.

## Report back

The exact lines changed and the final wording of the paragraph.
