# BRIEF ADDENDUM — narrow defect-prose corrections in `docs/explainers/rotor-noise-fit.qmd`

This addendum **extends** `.explainer-brief.md` by exactly one owned file. Everything in
`.explainer-brief.md` §0 (hard rules) still applies verbatim: no source edits, no sibling files,
no `git add`/`commit`/`stash`, no parent checkout, **no rendering, no quarto, no linters, no
tests**, no invented numbers, no claim that any candidate has been fitted or passed.

## Newly owned file

`docs/explainers/rotor-noise-fit.qmd` (2279 lines, now present in this worktree).

**This is a NARROW, SURGICAL assignment.** You are correcting a small set of specific
over-claims in the render/anti-aliasing defect prose so that this inherited document does not
contradict the scientifically honest revised explainers. **Do NOT rewrite unrelated historical
sections.** Do not restructure the document, do not touch the measured spectra, the fit tables,
the parameter discussions, the code cells, or any section that is not named below. Leave the
document's voice and numbering intact.

## The six corrections

### C1 — Remove the "every synthetic clip ever" over-claim (two places)

Line ~1673, inside the callout *"These renders exposed an aliasing bug, and it is fixed"*:

> "It was aliasing in the RENDER path, and it had been there for every synthetic clip this
> project has made."

and item 19 of the numbered findings list (~line 2233):

> "**Aliasing in every render, found on 2026-09-12 and fixed.**"

Both claims are **unsupported**. What was actually examined is the **stochastic fit-to-render
path** — `stage2.render_from_export` / `stage1_bayes.render_from_export` and the clips rendered
through it on this page. Nothing established that every synthetic clip the project has ever
produced went through that path.

**Fix:** restrict the scope to the examined stochastic fit-to-render path, naming it. Item 19's
heading must lose "in every render" and say what was examined instead. Do not generalise to
"every synthetic clip", "every render", "all synthetic audio", or any equivalent.

### C2 — "bit-for-bit" is not what was measured

Line ~1704:

> "and everything below 5 kHz is bit-for-bit what it was."

A band-power comparison does **not** establish sample-identical audio. **Fix:** state what was
actually measured — **band powers below 5 kHz unchanged at the reported precision**. Remove the
words "bit-for-bit" (and any equivalent such as "identical samples", "unchanged samples").

While you are in that sentence, note that "identical to the unfiltered path, so nothing the fit
modelled is lost" about the −4.9 dB tone reading is a **tone-level** statement at one frequency;
keep the number, but make the scope of the inference explicit rather than global.

### C3 — The 15.1 → 2.5 dB measurement used a different filter than the final one

The band measurement reported at lines ~1701–1706 (decimated 7.5–8 kHz band falling **15.1 → 2.5 dB**,
above-8 kHz contribution at **−148 dB**, 6–8 kHz **11.6 → 7.9 dB**) was taken with a **7800 Hz
passband** filter. It was **not** taken through the final **7900 Hz pass / 8000 Hz stop /
single-pass FIR** specification that the surrounding prose describes.

**Fix:** do **not** attribute those numbers to the final filter. Either

- **relabel them explicitly as a PRELIMINARY measurement** made with a 7800 Hz passband variant,
  stating that the final specification has not been re-measured and that these figures are
  therefore indicative of the mechanism, not of the shipped filter; **or**
- remove the numeric before/after and keep only the qualitative statement that the render-side
  low-pass removes the out-of-band fold-down.

**Prefer the first option** (relabel, keep the measurement with its true provenance) — the
project's convention is to preserve measured numbers with honest scope, not to delete them.
Whichever you choose, the text must no longer imply the shipped 7900/8000/100 dB filter produced
these figures.

Also correct the neighbouring description if it implies `filtfilt` or a two-pass/squared response
where the final path is a single centred FIR application: describe only what the document can
support, and mark anything not re-measured as unverified for the final path.

### C4 — Pre-correction gate decisions are historical, not current

The document contains gate/acceptance numbers recorded **before** the render correction — e.g. the
existing note "the numbers in §16.6 and §13 predate it" (~line 1710) and item 19's closing line
"Every gate number recorded before that date was measured on clips carrying this artefact."

**Fix:** make this consistent and explicit wherever such numbers are presented. Every gate decision
taken on pre-corrected renders must be flagged as **historical and unverified under the corrected
render path** — **not** as a current pass or a current fail. Do not re-derive, re-score or
re-interpret any of them. Do not delete them. A short `::: {.callout-warning}` stating the rule
once, plus pointers at the affected sections, is the right instrument; do not rewrite those
sections' content.

### C5 — Flag the absolute-level defect, publish no numbers for it

Main has identified a further specific defect on this path: the **stage 2 wrapper inherits
`srn.synthesize`'s `normalize_rms=0.1`**, which **erases the fitted absolute levels** from the
rendered output. The baseline worker is resolving physical units / no-normalization for the frozen
protocol; that work is **not yours** and is **not finished**.

**Fix:** add a short, clearly-scoped note (a callout) recording that this defect exists on the
examined render path, that fitted absolute levels are therefore not preserved through the wrapper
as it stands, and that the resolution is pending under the frozen protocol.

**You must NOT publish any new number for this.** No re-measured levels, no estimated error, no
before/after. Do not claim it is fixed. Do not claim any consequence for any existing result
beyond the plain statement that absolute levels are affected. New numbers wait for actual baseline
output.

### C6 — Consistency with the revised explainers

Where `rotor-noise-fit.qmd` states conclusions that the revised model supersedes, it already has a
scope note pointing at `bench-fit-revised-phase-model.qmd`. Ensure that pointer exists and is
accurate, and that nothing in the corrected passages contradicts:

- the raw-telemetry-reference / refined-diagnostic / realized-label semantics,
- the composite (not joint-NLL) scoring language,
- the statement that **no revised candidate has been fitted, scored or accepted**.

Do not otherwise reconcile this document — it is the historical record of the previous model, and
it stays that way.

## Flag, do not act

The image/audio assets under `docs/explainers/refit/` have been copied into this worktree so the
document is renderable later, but **rendering is out of scope now**. If you notice any other
over-claim outside the passages named above, **list it in your final report**; do not fix it.

## Done means

- `docs/explainers/rotor-noise-fit.qmd` corrected at C1–C5, with C6 verified.
- No other section of that document altered; `git diff --stat -- docs/explainers/rotor-noise-fit.qmd`
  shows a small, localized change.
- Greps confirming the removed claims are gone: `ever made`, `every synthetic`, `every render`,
  `bit-for-bit`.
- Nothing rendered, nothing linted, nothing committed.
