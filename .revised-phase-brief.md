# Baseline instrumentation worker brief — revised rotor-noise phase

You are the **baseline instrumentation / AA repair worker**. `Main` (parent agent, reachable
with the `hub` tool, id `Main`) owns all scientific design and all freeze decisions. You
implement. Do not redesign the science; propose and wait where this brief says so.

Do NOT commit this brief file (`.revised-phase-brief.md`). Leave it untracked.

## 0. Where you are, and what is already here

- cwd / your only writable checkout: `.worktrees/revised-phase`, branch `revised-phase`,
  branched from `f2d470d4`.
- **Never** edit the main checkout (`../..`) or any other worktree. Reading them is fine.
- The following uncommitted main-checkout work was snapshot-copied in for you and is
  already present as local modifications. It is inherited user work — preserve it, build
  on it, do not revert it:
  - `src/experiments/stochastic_fit/clips.py`
  - `src/experiments/stochastic_fit/stage1_bayes.py`
  - `src/experiments/stochastic_fit/stage2.py`
  - `scripts/_stage2_panels.py`
  - `scripts/_ab_render.py` (new)
  - `scripts/_fit_params_panel.py` (new)
  - `docs/handoff-stochastic-fit.md`
- `uv.lock` was deliberately **left at HEAD**. Never run `uv sync`, never touch `uv.lock`.
  `.venv`, `data`, `datasets`, `models`, `results/*` are symlinks shared with the main
  checkout — read them, never rewrite them.
- Two staged explainer deletions in main (`docs/explainers/bench-fit-derivations.qmd`,
  `docs/explainers/stage2-cruise.qmd`) were **not** replicated here. Ignore those files.

## 1. Scope — what you own

Own and edit only:
- `scripts/stochastic_fit_revised_eval.py` (new, the main deliverable)
- narrowly needed helpers inside `src/experiments/stochastic_fit/` (evaluation + render AA)
- regression tests for the above, in the project's existing test location/conventions

Explicitly **not** yours: any revised candidate model, any explainer `.qmd`, campaign docs,
raw data, refined labels, checkpoints/weights, dependency locks, the real-clip decimator.
No job submission and no baseline job until Main freezes the manifest. No heavy local
compute; no candidate training.

## 2. Deliverable A — the gate runner

Make this actually runnable:

```
python scripts/stochastic_fit_revised_eval.py --manifest <frozen-manifest> --check
```

It must persist the underlying metrics *and* the gate decisions, and exit nonzero when a
gate is unmet. Plus a baseline prepare/calibration path. Metrics are **exact identity and
full precision, paired**: pair on concrete recording/window identity, never by position or
modulo. Never convert previously rounded/median numbers into new intervals — recompute from
paired whole-recording data.

Reuse, do not copy:
- `scripts/_synthetic_probe.py::score` (line ~95) and the zoo/HPPNet/PIT scoring path in
  `scripts/_stage2_probe.py`, which already imports it.
- existing loaders, STFT and LTAS helpers in `src/experiments/stochastic_fit/`.

Frozen scorer: real-trained HPPNet L2 `hppnet_l2_r2_s0`, *best* checkpoint — pin and record
the actual checkpoint digest in the output.

### Gates

- **DREGON — cruise only.** PIT MAE of the frozen tracker on matched real vs
  baseline/candidate synthetic. Calibrate the positive real-vs-synthetic gap *first*.
  Point target `E_new <= E_real + 0.70 * (E_best_synth - E_real)`. A one-sided 95% interval
  clustered at recording level must exclude "no improvement". Clusters are independently
  recorded trajectory sessions — not aircraft, channels, windows or render seeds. Keep the
  overlap-aware held-out NLL improvement gate and the baseline-calibrated LTAS gate
  separate; no percentage NLL. No standby/ramp acceptance for DREGON.
- **Michael's — fixed-recording gate on FLY124.** Equally weighted candidate/baseline MAE
  ratios over standby, ramp and cruise; aggregate must be `<= 1.05`; report every regime
  separately. FLY125 is fit, FLY124 is held out. Any uncertainty statement is conditional on
  FLY124 — never an across-recording population-95 claim. The population-level 95% criterion
  was explicitly waived by the user.

## 3. Deliverable B — cohorts and baseline choice (Main has FROZEN this design)

Read the real exports, do not trust prose about them:
- `results/S2/cruise_8clip_refined.json` — `data.recordings == ["FLY125"]`,
  `dataset michaels-frames`, `rps_key rps_refined`, 8 x 16.0 s,
  `starts_s [16, 32, 48, 64, 96, 112, 128, 144]` (relative seconds).
- `results/S2/dregon_room2_cruise_refined.json` — `data.recordings ==
  ["free-flight_nosource_room2", "hovering_nosource_room2", "updown_nosource_room2",
  "rectangle_nosource_room2", "spinning_nosource_room2"]`, `dataset DREGON-frames`,
  `rps_key rps_refined`, 5 x 16.0 s.

Both are **real, newer refined-label fits**. The claim in the handoff doc that all exports
are raw-era is **stale** — verify against the artifacts themselves.

> **Timebase caveat you must handle:** DREGON `starts_s` are absolute epoch-style timestamps
> (e.g. `1512727417.205`) while Michael's are relative seconds (`16.0`). Support arithmetic
> and the window-support guard must handle both; do not assume one convention.

Frozen design:
- **Primary DREGON cohort** = exactly those five *no-source* room2 recordings, one 16 s fit
  window each. Do **not** add room1 speech/whitenoise recordings to the noise-only primary
  gate.
- Recorded fit supports are training/calibration. **Disjoint remaining >= 8 s cruise
  supports** are predictive evaluation, behind a window-support guard. If a recording cannot
  supply 8 s, **report it** — never pad, repeat or cycle.
- Pairing is whole-recording clustered: **5 units**.
- room1 noise-only may be a separately reported transfer diagnostic only — not primary, and
  not at all if there is no valid baseline mapping.
- **Baseline family selection:** compare raw vs refined **only on calibration supports**,
  then freeze **one** choice globally per rig/regime. No per-window winners. Match the
  DREGON per-record export by `data.recordings`; **no modulo cycling**.
- **Michael baseline:** the corresponding standby export for standby; the frozen global
  cruise export aggregate for cruise **and** ramp — an explicit, labelled current-model
  extrapolation, since ramp has no fitted baseline. One fixed choice across the whole
  held-out FLY124. Do not invent a ramp baseline.
- Where per-clip nuisance must be transferred, **average export per-clip nuisance in linear
  power**; never transfer an arbitrary last clip.
- A later candidate uses ONE joint parameter set per rig across all regimes, not independent
  per-regime vectors.

**Propose to Main and wait for approval before freezing:** the exact nuisance aggregation,
the baseline ramp extrapolation rule, the concrete cohort/support split, and the manifest
path + schema. Baseline validation jobs run only after the scientific schema is frozen.

## 4. Deliverable C — render anti-aliasing repair

Current state: `stage2.antialias` uses `kaiserord`/`firwin`/`filtfilt`, pass 7900, stop 8000,
attenuation 100 dB. Replace the render-only application with **one centered FFT /
overlap-add FIR application** (not `filtfilt`'s squared response, not the costly direct
path), with an explicit ripple + stop specification (7900 pass / 8000 stop / 100 dB stop),
preserved fit passband, and deterministic short-input boundary handling.

- Do **not** alter the real-clip decimator (`clips.decimate`) or the real clip path.
- Per-rotor masking was dropped by the user — do not reintroduce a mask.
- The renderer upstream may carry unrelated user edits / a taper: read it and preserve them.
- Add tone and transient regression tests.
- For an old/current oracle comparison use `git show 4ca88f68:<path>` — never mutate the
  checkout.
- Keep defect claims narrow to the stochastic-fit path you actually examined. No "every
  synthetic ever" claims, and do not describe band-power equality as bit-for-bit sample
  equality.

## 5. Timebase — RULING FROM MAIN, do not relitigate

`stage2.render_from_export` lines 366-370 already do
`n_native = int(round(rps.shape[-1] / SR * sample_rate_native))` plus interpolation onto the
native grid. **The rps16k -> native conversion is already present and correct.** Do NOT
"fix" it and do NOT force native-rate input. The contract's warning was *investigate*, not an
established defect. Investigate and report only; a duration/timebase regression test is
welcome as a descriptive check.

Do still inspect and fix the **export/window pairing** logic (identity matching by
`data.recordings`, no modulo cycling) — that is a genuine target.

## 6. Working rules

- **Skip all validation** — no project-wide format, lint, type-check or test-suite runs —
  until Main explicitly authorizes the final phase. Other agents are editing concurrently.
  Scoped proof of your own change (one test file, a targeted repro, a smoke run) is expected
  and fine.
- Provide regression tests that defend observable contracts: gate arithmetic and threshold
  boundaries, pairing identity, the support guard's report-not-pad behaviour, AA tone/
  transient response, short-input determinism. No tests that merely pin wording or wiring.
- Escalate to Main via `hub` (do not block on it unless you truly cannot proceed) if: the
  DREGON gap is indistinguishable from zero, required real coverage is absent, the
  independent DREGON recordings are insufficient, or you hit unapproved scope, risk or
  material ambiguity.
- Report what you actually ran and observed. Never claim unverified success.
