# Addendum 4 — Main's review of your `revised_eval.py`: two REQUIRED changes

Precedence: addendum 4 > addendum 3 > addendum 2 > addendum > brief. Main reviewed the module
you have written so far. Two findings, both confirmed in the source. Neither is optional.

## 23. `predicted_m` is NOT the candidate's observation — label it or share the kernel

Your `predicted_m` (revised_eval.py ~725) drives `model.CombSpectrum` (~633, ~658) off the
frame-mean RPS plus a chirp-width term. That is **not** the exact moving-window kernel. The
candidate path will use exactly integrated moving atoms.

Pick one of these two, explicitly:

1. **Keep it, but label it honestly** as a *historical-forward baseline diagnostic* — the old
   descriptive model's own forward prediction — in the docstring, in `__all__` exposure and in
   the saved metrics record; **or**
2. **Replace it with the shared kernel** (preferred, and now available):
   `src/experiments/stochastic_fit/phase_kernel.py::expected_periodogram_from_atoms(atoms, r_tau, *, n_fft, window_sumsq)`
   owned by the candidate worker and frozen. Conventions: `L = 2*n_fft`; acf by zero-padded
   FFT; n_fft-grid bin `j` is L-bin `2j` (exact subsample, no interpolation); image fold
   `M[j] = 0.25*(P_Z[2j] + P_Z[(L-2j) mod L])/window_sumsq`; DC and Nyquist self-map so they
   emerge at `0.5*P_Z/window_sumsq`; interior bins are NOT doubled, matching
   `data.periodogram`. `r_tau` is consumed verbatim — fold your two-component weights into it,
   it will not be renormalized. Your broadband floor stays additive in power on your side.

**Do NOT** freeze option 1 while describing it as the identical physical observation to the
candidate's. The discrepancy matters most for the **Michael ramp**, where a frame-mean speed
plus a widening heuristic is exactly the approximation the contract rejects. Whichever you
choose, state the choice in the proposals doc.

## 24. Absolute level is currently ERASED — blocks absolute LTAS

Confirmed defect. `stage2.render_from_export` calls `srn.synthesize(...)` with no
`normalize_rms` override, and `stochastic_rotor_noise.synthesize` defaults
`normalize_rms: float | None = 0.1` (source line 1429), applied at lines 1624-1626 as
`gain = normalize_rms / rms`. Every rendered clip is therefore rescaled to a fixed RMS and the
**absolute fitted levels are destroyed**.

This **must** be fixed before any absolute-LTAS or multi-regime comparison — i.e. before the
addendum-3 §17 gate can mean anything.

Requirements:

- **Do not change the existing default.** `synthesize(normalize_rms=0.1)` and the
  `StochasticRotorNoise` class API (~1720, ~1803, ~1978) serve unrelated callers
  (`_stage2_probe.py`, `_stage2_panels.py`, `_ab_render.py`, training pipelines). Preserve the
  arbitrary-RMS behaviour for them.
- Thread an explicit keyword through `render_from_export` whose **default preserves today's
  behaviour**, and have the evaluator pass the physical-units setting (`normalize_rms=None`)
  explicitly. The evaluator/experimental wrapper is the only physical-units, no-arm-
  normalization path.
- **Folding `scores.power_scale` into `profile_db` is NOT sufficient.** Derive the actual
  conversion from stored fit power to renderer units and declare it:
  - data convention: `I = |FFT|^2 / sum(w^2)`;
  - integrated one-sided `I` over the band relates to signal variance as `Fs/2 * variance`.
  - Validate with a **pure-tone** conversion test and a **floor** conversion test — planted
    known amplitude in, expected `I` out. No silent factor anywhere.
- **Legacy exported level numbers are STALE**: they were produced under RMS normalization.
  Do not treat old absolute levels as physical. Say so where they are consumed.
- Report **actual adapter-vs-renderer results** (your "predicted_m vs renderer mean" and
  "adapter verification, frame-averaged" cells) with the declared conversion applied. Numbers,
  not assurances.

## 26. Regime scoring support — score the declared interval, not the whole window

The Michael **ramp can be shorter than an 8 s context window**. Therefore:

- Score only the outputs whose **times lie inside the pre-registered raw-telemetry ramp
  support**. The surrounding waveform is **context only**.
- **Never** label a mostly-standby/cruise 8 s average as "ramp MAE". That is the single
  easiest way to fake this gate.
- Standby and cruise are likewise scored on their **declared support**. The whole-window
  context plus the complete STFT / filter guard may legitimately extend beyond the scored
  target interval — that is expected, and is not licence to score the extra material.
- **Regime definitions are fixed and selected BEFORE any candidate.** Never infer regime
  boundaries from a candidate, nor from a refined/posterior track.
- **Reuse the existing output-grid alignment and PIT helper.** No new model, no new metric.
- Retain the masks and the scored times as **provenance** in the saved record, so a reader can
  check which samples entered which regime's MAE.

## 25. Reminder

Still no baseline job, no freeze, no project-wide validation until Main authorizes. Record the
§23 choice and the §24 declared conversion in `docs/revised-phase-baseline-proposals.md`.
