*.pyc
# data/ and datasets/ contents are managed by dload (PyPI dload-ml); the
# remote + pinned dataset versions live in dload.toml / dload.lock at repo root.
/data/*
/datasets/*
wandb/
results/
# dload / online-mix / download scratch caches
.cache/
# zoo checkpoint index (R2 is the source of truth; this is a local cache)
.checkpoints-cache.json
# omnirun shared worktrees
.trees/
.direnv/
.env
*.xml
.coverage

drones-with-speeds.md
.ipynb_checkpoints/
.claude/worktrees/
.worktrees/**
!.worktrees/.gitkeep

bibliography/**.png

.venv/
node_modules/

# Trello project config (may contain workspace names)
.trello

# nix build result
result

# Hydra run output dirs (auto-created by train.py/eval.py)
outputs/

# some old stuff
backups/

# Auto-generated by git-hooks.nix
/.pre-commit-config.yaml
omnirun-outputs/
.jupyterlab.log
# per-checkout direnv extras (see .envrc)
.envrc.local
docs/explainers/*.html
